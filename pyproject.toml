[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools", "wheel"]

[tool.black]
line-length = 120

[tool.isort]
line_length = 120
profile = "black"

[tool.pytest]
addopts = "--cov --cov-report html"
testpaths = "tests/"
